name: pseudo_terminal_utils
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dart_pty:
    git: https://github.com/Termare/dart_pty
  global_repository:
    git:https://github.com/shoaibhassanking059/global_repository
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
